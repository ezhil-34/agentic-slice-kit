================================================================================
TEST REPORT: tests/test_integration.py
"Does it actually work against the real model, not just fakes?"
================================================================================

SUMMARY:
- Target Component : Live OpenRouter LLM Integration (slice/llm.py & demo/tracker/)
- Total Tests      : 4 passed out of 4
- Status           : ALL PASSED (100%)
- Requirement      : Skipped automatically if OPENROUTER_API_KEY environment variable is absent.

PURPOSE & OVERVIEW:
This test file executes live integration calls against OpenRouter API endpoints 
using strict Pydantic JSON schema constraints. It validates:
  1. Live Schema Adherence: Real LLM output strictly matches ErrorClassification Pydantic model.
  2. Token Counting: Live LLM call records actual tokens spent into Budget.
  3. ReExplanation Schema: Real LLM output strictly matches ReExplanation schema.
  4. Mini End-to-End Flow: Full live run involving wrong answer, classification, 
     re-explanation, and correct completion.

--------------------------------------------------------------------------------
TEST FUNCTION BREAKDOWN:
--------------------------------------------------------------------------------

1. test_live_classify_returns_valid_schema
   - Sends live classification request to OpenRouter; verifies output parses cleanly into ErrorClassification.

2. test_live_call_records_tokens
   - Verifies budget.tokens_used() increases after a live OpenRouter complete() API call.

3. test_live_reexplain_returns_valid_schema
   - Sends live re-explanation request; verifies output contains valid strategy and non-empty explanation text.

4. test_mini_flow_draft_classify_complete
   - Runs full mini-flow against live API:
     * Wrong answer submitted
     * Collision handled via method check
     * Classification & re-explanation generated by live LLM
     * Correct answer submitted
     * Verifies tokens > 0 and run completes successfully.

================================================================================
